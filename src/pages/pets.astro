---
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../consts";
import { getCollection } from "astro:content";
import PetTile from "../components/PetTile.astro";
import { ZodUndefined } from "astro:schema";

const pinned_pets = await getCollection("pets", ({ data }) =>
    data.is_pinned?.valueOf(),
);
const rest_pets = await getCollection(
    "pets",
    ({ data }) => !data.is_pinned?.valueOf(),
);
---

<!doctype html>
<html lang="en">
    <head>
        <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
        <style>
            main {
                width: 960px;
            }
            ul {
                display: flex;
                flex-wrap: wrap;
                gap: 2rem;
                list-style-type: none;
                margin: 0;
                padding: 0;
            }
            ul li {
                width: calc(90% - 1rem);
            }
            ul li * {
                text-decoration: none;
                transition: 0.2s ease;
            }
            ul li a:hover h4,
            ul li a:hover .date {
                color: rgb(var(--accent));
            }
            ul a:hover img {
                box-shadow: var(--box-shadow);
            }
            @media (max-width: 768px) {
                ul {
                    gap: 0.5em;
                }
                ul li {
                    width: 100%;
                }
            }
        </style>
    </head>
    <body>
        <Header />
        <main>
            <blockquote>
                Programming languages are just insruments. Why choose the
                universal one, when you can choose the better suited one each
                time?
            </blockquote>
            <section>
                <ul>
                    {
                        pinned_pets.map((project) => (
                            <PetTile project={project} />
                        ))
                    }
                </ul>
            </section>
            <section>
                <ul>
                    {rest_pets.map((project) => <PetTile project={project} />)}
                </ul>
            </section>
        </main>
        <Footer />
    </body>
</html>
